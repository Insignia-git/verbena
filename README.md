# verbena
# Wdrożenie strony konkursowej (Supabase + statyczny hosting)
echo "# verbena" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/Insignia-git/verbena.git
git push -u origin main
